/*This is my app.js*/

var elem = document.querySelector('.tabs');
     var options = {};
     var instance = M.Tabs.init(elem, options);

var instance = M.Tabs.init(elem, options);

console.log(instance);
